**Fixes**
- Complication icons show on icon + text watch face slots
- Watch save no longer creates duplicate items and returns to the list after saving
- Sync button only appears when the paired phone has a matching app (debug with debug, release with release)
